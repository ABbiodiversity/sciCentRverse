% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_calc_density_by_loc.R
\name{cam_calc_density_by_loc}
\alias{cam_calc_density_by_loc}
\title{Calculate density at each location from seasonal time and EDD (with optional pooled fallback)}
\usage{
cam_calc_density_by_loc(
  duration_df,
  edd_category_df,
  model_df = NULL,
  cam_fov_angle = 40,
  format = c("long", "wide"),
  include_project = TRUE,
  height_col = "height",
  dist_groups_df = NULL,
  edd_df = NULL,
  aggregate = FALSE,
  use_global_edd = FALSE,
  annotate_edd_source = TRUE
)
}
\arguments{
\item{duration_df}{Tibble with: project, location, species_common_name, season,
total_season_days, total_duration (seconds). If present, \code{model} and/or \code{height}
are used directly.}

\item{edd_category_df}{Tibble with vegetation/EDD category per camera; must contain
\code{overall_category} plus (\code{project},\code{location}) or \code{project_location}.}

\item{model_df}{Optional tibble (\code{project},\code{location},\code{model}) to attach if \code{duration_df}
lacks a \code{model} column.}

\item{cam_fov_angle}{Camera FOV in degrees. Default 40.}

\item{format}{"long" (default) or "wide".}

\item{include_project}{Keep \code{project} in wide output. Default TRUE.}

\item{height_col}{Column name in \code{duration_df} to use as height (default "height").}

\item{dist_groups_df}{Optional override mapping (species_common_name -> dist_group).
If NULL, an object named \code{dist_groups} must exist in your env/package.}

\item{edd_df}{Optional override EDD table. If NULL, an object named \code{edd} must exist.
Must include: dist_group, season, overall_category, edd; ideally \code{n}, and optionally \code{model}, \code{height}.}

\item{aggregate}{Logical; if TRUE, return weighted mean per deployment by species
(weights = \code{total_season_days}) after removing Bear–winter rows.}

\item{use_global_edd}{Logical; if TRUE, fill missing exact EDDs using a \strong{dist-group–specific plan}.}

\item{annotate_edd_source}{Logical; add \code{edd_source} = "exact"/"pooled" (default TRUE).}
}
\value{
Seasonal densities (long/wide) or weighted aggregate (if \code{aggregate = TRUE}).
}
\description{
Computes seasonal (or aggregated) density estimates per deployment × species using
time-in-front-of-camera (TIFC) totals and effective detection distance (EDD)
lookups. Supports exact EDD matches on species group, vegetation category,
season, and—when available—camera model and height. Optionally applies a
dist-group–specific pooled fallback when exact EDDs are missing. Note that filtering
locations with total days below a certain threshold (e.g., 30 days) should be done
prior to running the this function.
}
\details{
\strong{Inputs and joins}
\itemize{
\item \code{duration_df} must contain: \code{project}, \code{location}, \code{species_common_name},
\code{season}, \code{total_season_days} (days of operation in that season), and
\code{total_duration} (seconds of TIFC in that season). If present, \code{model}
and/or \code{height} are used directly.
\item \code{edd_category_df} must provide \code{overall_category} for each (\code{project}, \code{location})
(or via \code{project_location} that gets split).
\item Species are mapped to EDD groups via \code{dist_groups} (override with \code{dist_groups_df}).
\item EDDs are taken from \code{edd} (override with \code{edd_df}). Exact joins use keys:
\code{dist_group}, \code{overall_category}, \code{season}, and, when present in both
data and lookup, \code{model} and/or \code{height}. If multiple EDD rows share the
same keys, the entry with the largest \code{n} is chosen.
}

\strong{Density calculation}
\itemize{
\item Area per season (m²): \code{area_m2 = π * (edd^2) * (cam_fov_angle / 360)}.
\item Effort per season (m²·10⁻²): \code{effort = total_season_days * (area_m2 / 100)} (legacy factor).
\item CPUE: \code{cpue = total_duration / effort}.
\item Density (km⁻²): \code{density_km2 = (cpue / 86400) * 10000}.
\item If \code{edd}, \code{effort}, or \code{total_season_days} are missing/zero, \code{density_km2} is \code{NA}.
}

\strong{Pooled EDD fallback (\code{use_global_edd = TRUE})}
Uses weighted pooled EDDs (weights = \code{n}) following a conservative plan per \code{dist_group}:
\itemize{
\item \code{Coyote}, \code{Deer}, \code{LargeUngulates}: pool over \code{overall_category}
(keys: \code{dist_group}, \code{season}, \code{model}, \code{height}; absent keys are ignored).
\item \code{Bear}, \code{Hare}, \code{Lynx}, \code{SmallMustelids}, \code{Wolf}: try pooled-overall first, then pool over height.
If pooling supplies an EDD and \code{annotate_edd_source = TRUE}, marks \code{edd_source = "pooled"};
otherwise \code{edd_source = "exact"}. If pooling fails, density remains \code{NA}.
}

\strong{Aggregation (\code{aggregate = TRUE})}
\itemize{
\item Returns one row per deployment × species with \code{weighted.mean(density_km2, w = total_season_days, na.rm = TRUE)}.
\item Removes Bear–winter rows before aggregation (including zeros).
}

\strong{Outputs}
\itemize{
\item \code{format = "long"}: per-season rows with \code{overall_category}, \code{model}, \code{height},
\code{total_season_days}, \code{total_duration}, \code{density_km2}, and (optionally) \code{edd_source}.
\item \code{format = "wide"}: densities pivoted to one row per deployment (diagnostic season-day columns included).
\item \code{aggregate = TRUE}: one row per deployment × species (no seasons).
}
}
\seealso{
\code{\link[=cam_sum_total_time]{cam_sum_total_time()}}, \code{\link[=cam_calc_time_by_series]{cam_calc_time_by_series()}}, \code{\link[=cam_summarise_op_by_season]{cam_summarise_op_by_season()}}
}
\author{
Marcus Becker
}
