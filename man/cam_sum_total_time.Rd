% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_sum_total_time.R
\name{cam_sum_total_time}
\alias{cam_sum_total_time}
\title{Summarise total time by project, location, species, and season (with op-days)}
\usage{
cam_sum_total_time(
  series_df,
  season_cutoffs = c(spring = 99L, summer = 143L, winter = 288L),
  tz = Sys.timezone(),
  op_days_df,
  species_universe = NULL
)
}
\arguments{
\item{series_df}{Output of cam_calc_time_by_series(); needs
project, location, species_common_name, series_start, series_total_time}

\item{season_cutoffs}{Named integer vector of Julian cutoffs, e.g.
c(spring=99L, summer=143L, winter=288L). Labels come from names (lower-case
recommended for consistency with EDD lookups). Cutoffs are auto-sorted, so
order does not matter.}

\item{tz}{Time zone used to extract the Julian day from \code{series_start}.
Defaults to \code{Sys.timezone()} (the local system timezone). WildTrax
timestamps are typically stored in local time, so this default is usually
correct. Override explicitly (e.g. \code{"America/Edmonton"}) when running
on a machine whose timezone does not match the study area.}

\item{op_days_df}{Operating-days table (required) for zero-fill + days per
season. Accepts EITHER:
\itemize{
\item long: project, location, season, total_season_days (or operating_days), or
\item wide: project, location, one column per season label (values are day counts).
Typically the output of \code{cam_summarise_op_by_season(wide = TRUE)}.
}}

\item{species_universe}{Optional vector used for zero-fill across species}
}
\value{
Tibble with: project, location, species_common_name, season,
total_duration, total_season_days
}
\description{
Rolls up image-series time (seconds) to seasonal totals per deployment × species
and attaches the number of operating days per season. Seasons are assigned by
Julian cutoffs you provide (e.g., spring = 99, summer = 143, winter = 288).
Output is ready to feed into density calculations that require both
\code{total_duration} and \code{total_season_days}.
}
\details{
\strong{Season assignment}
\itemize{
\item Each series row is assigned to a season using the Julian day of \code{series_start}
in the chosen \code{tz}. Cutoffs must be strictly increasing; season labels come
from the names of \code{season_cutoffs} and are used downstream (keep them lower-case
for consistency with EDD lookups).
}

\strong{Aggregation}
\itemize{
\item Within each \code{project} × \code{location} × \code{species_common_name} × \code{season},
the function sums \code{series_total_time} to produce \code{total_duration} in seconds.
}

\strong{Operating days}
\itemize{
\item \code{op_days_df} can be supplied either in long form (has a \code{season} column and a
day-count column named \code{total_season_days} or \code{operating_days}) or wide form
(one column per season label). The function normalizes this to a long table and
merges to add \code{total_season_days}.
}

\strong{Zero-filling}
\itemize{
\item To ensure locations with no detected series still appear (with 0 duration),
the function constructs the full grid of cameras (from \code{op_days_df}) × seasons,
crossed with a species universe. If \code{species_universe} is not supplied, it uses
the species present in \code{series_df}. This guarantees downstream density steps
see explicit zeros rather than missing rows.
}

\strong{Input requirements}
\itemize{
\item \code{series_df} must include: \code{project}, \code{location}, \code{species_common_name},
\code{series_start} (POSIXct or parseable), and \code{series_total_time} (seconds).
\item \code{op_days_df} must provide operating-day counts per camera × season (long or wide).
}

\strong{Output}
\itemize{
\item A tibble with columns:
\code{project}, \code{location}, \code{species_common_name}, \code{season},
\code{total_duration} (seconds), and \code{total_season_days} (days).
}
}
\examples{
\dontrun{
# series is the output of cam_calc_time_by_series()
# op_days is the output of cam_summarise_op_by_season(wide = TRUE)
dur <- cam_sum_total_time(
  series_df        = series,
  season_cutoffs   = c(spring = 99L, summer = 143L, winter = 288L),
  op_days_df       = op_days,
  species_universe = c("White-tailed Deer", "Moose", "Black Bear")
)
}

}
\seealso{
\code{\link[=cam_calc_time_by_series]{cam_calc_time_by_series()}}, \code{\link[=cam_get_op_days]{cam_get_op_days()}}, \code{\link[=cam_summarise_op_by_season]{cam_summarise_op_by_season()}}
}
\author{
Marcus Becker
}
