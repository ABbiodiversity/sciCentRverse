% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_summarise_op_by_season.R
\name{cam_summarise_op_by_season}
\alias{cam_summarise_op_by_season}
\title{Summarise operational days by user-defined seasons}
\usage{
cam_summarise_op_by_season(
  calendar_df,
  grouping = c("project", "project_id", "location", "location_id"),
  seasons = c(spring = 99, summer = 143, winter = 288),
  labels = NULL,
  date_col = "date",
  operating_col = "operating",
  na_as = FALSE,
  by_year = FALSE,
  wide = TRUE
)
}
\arguments{
\item{calendar_df}{A tibble/data.frame with at least \code{date} (Date) and
\code{operating} (logical/NA), typically the output of \code{cam_get_op_days()}.}

\item{grouping}{Character vector of grouping columns to keep in the summary.
Defaults to \code{c("project","project_id","location","location_id")}; only columns present
are used. If none are present, aggregation is global.}

\item{seasons}{Season starts. Named numeric, unnamed numeric + \code{labels}, or a
data frame with \code{season} and \code{start} columns. Defaults to
\code{c(spring = 99, summer = 143, winter = 288)}.}

\item{labels}{Optional labels if \code{seasons} is an unnamed numeric vector.}

\item{date_col}{Name of the date column in \code{calendar_df}. Default \code{"date"}.}

\item{operating_col}{Name of the logical operating column. Default \code{"operating"}.}

\item{na_as}{Logical: treat \code{NA} in \code{operating} as TRUE when counting?
Default \code{FALSE}.}

\item{by_year}{Logical: also summarise by calendar year? Default \code{FALSE}.}

\item{wide}{Logical: return one column per season (\code{TRUE}) or a long table with
\code{season} and \code{operating_days} (\code{FALSE}). Default \code{TRUE}.}
}
\value{
A tibble. In \strong{wide} mode: grouping cols (+ \code{year} if \code{by_year}),
one column per season (counts), and \code{total_days} (row sum). In \strong{long} mode:
grouping cols (+ \code{year}), \code{season}, \code{operating_days}, and \code{total_days}.
}
\description{
Takes the output from \code{cam_get_op_days()} and sums the number of
\strong{operational days} per season. Seasons are defined
by Julian day \strong{start} cutoffs and can be 1..n segments that wrap the year.
}
\details{
\strong{Season definitions} (\code{seasons}) can be supplied in any of these forms:
\itemize{
\item \strong{Named numeric vector} (recommended): e.g.,
\code{c(spring = 99, summer = 143, winter = 288)}.
\item \strong{Unnamed numeric + \verb{labels=}}: e.g., \verb{seasons = c(99, 143, 288), labels = c("spring","summer","winter")}.
\item \strong{Data frame} with columns \code{season} (labels) and \code{start} (Julian day).
}

Rules:
\itemize{
\item Starts must be integers in \verb{1..366}, unique, and will be sorted.
\item Classification is circular: the last season runs to day 366, then wraps to the first start.
}

\strong{Grouping}: By default the function looks for \verb{c("project","project_id","location", "location_id)}
but only use the ones actually present in \code{calendar_df}. If none are present,
the result is aggregated across all records.

\strong{Operating NA handling}: By default \code{operating = NA} is counted as \code{FALSE}
(\code{na_as = FALSE}). Set \code{na_as = TRUE} to count unknown days as operational.
}
\examples{
\dontrun{
cal <- cam_get_op_days(image_reports,
                       grouping = c("project_id","location","location_id"),
                       span = "operational", missing_as = FALSE)

# Default seasons, by year, wide format
sum1 <- cam_summarise_op_by_season(cal, by_year = TRUE, wide = TRUE)

# Two-season example, long format
sum2 <- cam_summarise_op_by_season(
  calendar_df = cal,
  seasons     = c(IceFree = 120, FreezeUp = 305),
  by_year     = FALSE,
  wide        = FALSE
)
}

}
\seealso{
\code{\link{cam_get_op_days}}
}
\author{
Marcus Becker
}
