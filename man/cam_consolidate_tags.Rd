% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_consolidate_tags.R
\name{cam_consolidate_tags}
\alias{cam_consolidate_tags}
\title{Consolidate per-image species tags into one row from a WildTrax main report}
\usage{
cam_consolidate_tags(report)
}
\arguments{
\item{report}{A data.frame/tibble with at least:
\verb{project_id, location, location_id, image_id, image_date_time, species_common_name, individual_count, age_class, sex_class, tag_id}.
Should be a main report from WildTrax.}
}
\value{
A tibble with one row per image × species:
\verb{project_id, location, location_id, image_id, image_date_time, species_common_name, age_class, sex_class, individual_count} (character).
}
\description{
For each \verb{(project_id, location, location_id, image_id, species_common_name)}
group, sum \code{individual_count} (numeric rows) and collapse \code{age_class} /
\code{sex_class} by replicating each tag by that row's count and joining with
\code{", "}. The \code{image_date_time} is retained and must be identical within each
image. Non-species rows (e.g., \code{"STAFF/SETUP"}, \code{"NONE"}) are preserved unchanged.
Rows whose \code{species_common_name} is neither in \code{native_sp} nor in the
non-species list are \strong{dropped with a warning} listing the unexpected labels
and row counts.
}
\details{
Species identification uses the \strong{internal} character vector \code{native_sp}
saved in \code{R/sysdata.rda}.

The function also \strong{detects, warns about, and removes} likely duplicate tag
entries where all fields are identical \strong{except} \code{tag_id},
which is a possible double-entry flag.
The duplicate check is applied to species rows only, using these fields:
\verb{project_id, location, location_id, image_id, image_date_time, species_common_name, individual_count, age_class, sex_class}.
When duplicates are found, the function keeps \strong{one} row per identical set
(the one with the \strong{lowest \code{tag_id}}) and drops the rest.
}
\examples{
\dontrun{
x <- cam_consolidate_tags(main_reports)
# If duplicate tags were removed, you'll see a warning listing image_id(s).
}

}
\seealso{
\code{\link[=cam_calc_time_by_series]{cam_calc_time_by_series()}} for the next step in the pipeline.
}
\author{
Marcus Becker
}
