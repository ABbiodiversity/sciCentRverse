% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_extract_model_lookup.R
\name{cam_extract_model_lookup}
\alias{cam_extract_model_lookup}
\title{Extract per-camera model ("hf2" / "pc900") from an image report}
\usage{
cam_extract_model_lookup(
  image_report,
  keys = c("project", "location"),
  model_col = "equipment_model",
  hf2_pattern = "HF2",
  pc900_pattern = "PC900|PC 900|PC-900"
)
}
\arguments{
\item{image_report}{Data frame with at least \code{keys} and the model column.
Typically a WildTrax image report.}

\item{keys}{Character vector of columns that identify a camera. Default:
c("project","location"). If you prefer per-\code{location_id}, include it
(e.g., c("project","location","location_id")).}

\item{model_col}{Column name (string or bare) containing the raw model text.
Default "equipment_model".}

\item{hf2_pattern}{Regex used to detect HF2 models (case-insensitive).
Default "HF2".}

\item{pc900_pattern}{Regex used to detect PC900 models (case-insensitive).
Default "PC900|PC 900|PC-900".}
}
\value{
Tibble with \code{keys} + \code{model} (values: "hf2" or "pc900").
}
\description{
Minimal helper to derive a per-camera \code{model} from a model column
(defaults to \code{equipment_model}). Returns one row per camera key with
\code{model} in lower case.
}
\author{
Marcus Becker
}
