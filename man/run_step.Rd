% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_step.R
\name{run_step}
\alias{run_step}
\title{Run a Pipeline Step with Logging}
\usage{
run_step(label, script)
}
\arguments{
\item{label}{Character string. A descriptive label for the pipeline
step, used in console output and log file naming.}

\item{script}{Character string. The filename of the R script to
execute, relative to \code{code_dir}.}
}
\value{
NULL (invisibly). Output is printed to console and/or log
file.
}
\description{
Executes an R script as part of a data processing pipeline, with
optional logging to file and execution time tracking.
}
\details{
This function:
\itemize{
\item Prints the step label and timing information to console
\item Creates a timestamped log file in \code{2_pipeline/logs/}
directory
\item Redirects both standard output and messages to the log file
\item Sources (executes) the specified script from \code{code_dir}
\item Calculates and displays the execution duration
\item Automatically closes log file connections via \code{on.exit()}
}

Log files are named using the pattern:
\code{YYYY-MM-DD_HH-MM-SS_<sanitized_label>.log}
Label sanitization converts non-alphanumeric characters to
underscores.
}
\note{
Requires the global variable \code{code_dir} to be defined. Requires the
function \code{format_time_diff()} to be available. Logging is controlled
by the \code{enable_logging} variable.
}
\examples{
\dontrun{
code_dir <- "1_code/r_scripts"
run_step("Data Import", "01_import_data.R")
run_step("Data Cleaning", "02_clean_data.R")
}
}
