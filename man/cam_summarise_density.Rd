% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_summarise_density.R
\name{cam_summarise_density}
\alias{cam_summarise_density}
\title{Summarise density across locations within an area of interest, with uncertainty}
\usage{
cam_summarise_density(
  density_df,
  group_col,
  species_col = "species_common_name",
  dens_col = "density_km2",
  agg_samp_per = TRUE,
  samp_per_col = NULL,
  conf_level = 0.9,
  n_sim = 10000,
  remove_na = TRUE,
  seed = NULL
)
}
\arguments{
\item{density_df}{Tibble/data.frame of location-level density estimates, e.g.
the output of \code{\link[=cam_calc_density_by_loc]{cam_calc_density_by_loc()}}. If an \code{sf} object, geometry is dropped.}

\item{group_col}{Column name(s) identifying the unit/area of interest to
summarise within (e.g., \code{"grid"}). Character vector of one or more columns.}

\item{species_col}{Column name identifying species. Default \code{"species_common_name"}.}

\item{dens_col}{Column name of location-level density values. Default \code{"density_km2"}.}

\item{agg_samp_per}{Logical; if \code{TRUE} (default), sampling periods are pooled
before summarising. If \code{FALSE}, output retains one row per sampling period
(see \code{samp_per_col}).}

\item{samp_per_col}{Optional column name identifying the sampling period. If
\code{NULL} (default) and \code{agg_samp_per = FALSE}, a default column named
\code{"samp_per"} is used if present.}

\item{conf_level}{Confidence level for the simulated interval. Default \code{0.9}.}

\item{n_sim}{Number of simulation draws used to build the confidence interval.
Default \code{10000}.}

\item{remove_na}{Logical; drop rows with \code{NA} in \code{dens_col} before summarising?
Default \code{TRUE}.}

\item{seed}{Optional integer seed for reproducible simulation. The global RNG
state is saved and restored, so calling this function does not affect
random draws elsewhere in the user's session. Default \code{NULL} (no seed set).}
}
\value{
A tibble grouped by \code{group_col} (and \code{samp_per_col} if applicable) and
\code{species_col}, with columns \code{n_deployments}, \code{occupied}, \code{prop_occupied},
\code{density_avg}, and the lower/upper confidence bounds named
\verb{density_lci_<conf_level>} / \verb{density_uci_<conf_level>}.
}
\description{
Takes location-level density estimates (e.g., the output of
\code{\link[=cam_calc_density_by_loc]{cam_calc_density_by_loc()}}) and calculates a mean density per species within
a user-defined unit of interest (e.g., a grid, cluster, or region). Because
density at unsampled/unoccupied locations is exactly zero, the mean is
calculated as a two-part ("hurdle") estimator: the proportion of locations
occupied, multiplied by the average density at occupied locations. Confidence
intervals are obtained by simulating from the sampling distributions of both
components and combining them.
}
\details{
\strong{Two-part mean.} For a given group (area of interest x species):
\itemize{
\item \code{prop_occupied} = proportion of locations with \code{dens_col > 0}.
\item \code{agp} ("abundance given presence") = mean of \code{dens_col} among occupied locations.
\item \code{density_avg} = \code{prop_occupied * agp}.
}

\strong{Uncertainty.} \code{prop_occupied} is treated as a binomial proportion
(\code{n_deployments} trials), and \code{agp} is treated as log-normal, with the standard
deviation of \code{log(dens_col)} among occupied locations scaled by
\code{1/sqrt(occupied)} to approximate the standard error of the mean. \code{n_sim} draws
are taken from each distribution and multiplied together to build a simulated
distribution of \code{density_avg}, which is then rescaled to have mean equal to the
point estimate before taking quantiles at \code{conf_level} to form the confidence
interval. This is a simulation-based (parametric bootstrap) approach to
combining the uncertainty of the two components — related to, but not
identical to, an analytical delta-method variance formula.

When a group has zero occupied locations, \code{density_avg}, the lower CI, and the
upper CI are all set to 0. When a group has exactly \strong{one} occupied location,
there is no information available on the variance of \code{agp}; the simulation
still incorporates uncertainty in \code{prop_occupied}, but \code{agp} is held fixed at
its point estimate.

\strong{Grouping.} \code{density_df} is always grouped by \code{group_col} and \code{species_col}.
If \code{samp_per_col} is supplied (or a default \code{samp_per} column exists) and
\code{agg_samp_per = FALSE}, the output retains one row per sampling period;
otherwise sampling periods are pooled together before summarising.

\strong{Missing density values.} Rows where \code{dens_col} is \code{NA} provide no
information on occupancy status and are dropped prior to summarising (see
\code{remove_na}); by default a message reports how many rows were dropped.
}
\examples{
\dontrun{
# density is the output of cam_calc_density_by_loc(), split into a grouping
# column (e.g., grid) and location
density |>
  tidyr::separate(location, into = c("grid", "station"), sep = "-") |>
  cam_summarise_density(
    group_col    = "grid",
    species_col  = "species_common_name",
    dens_col     = "density_km2",
    agg_samp_per = TRUE,
    conf_level   = 0.9
  )
}

}
\seealso{
\code{\link[=cam_calc_density_by_loc]{cam_calc_density_by_loc()}}
}
\author{
Marcus Becker
}
