% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cam_obtain_n_gap_class.R
\name{cam_obtain_n_gap_class}
\alias{cam_obtain_n_gap_class}
\title{Identify "N" gap boundaries where a NONE is found between animal images}
\usage{
cam_obtain_n_gap_class(
  cons_main_report,
  grouping = c("project", "project_id", "location", "location_id"),
  datetime_col = "image_date_time",
  species_col = "species_common_name",
  none_label = "NONE",
  max_bridge_secs = 120,
  require_same_species = TRUE
)
}
\arguments{
\item{cons_main_report}{Main report with consolidated tags (e.g., from
\code{cam_consolidate_tags()}), requiring: \code{image_date_time}, \code{image_id},
\code{species_common_name}, and camera identity columns.}

\item{grouping}{Character vector of camera identity columns. Only those
present are used. Default: c("project","project_id","location","location_id").}

\item{datetime_col}{Name of the timestamp column. Default "image_date_time".}

\item{species_col}{Name of the species column.   Default "species_common_name".}

\item{none_label}{Label denoting a NONE image. Default "NONE".}

\item{max_bridge_secs}{Numeric, maximum animal-to-animal elapsed seconds to still
consider a bridged series. Default 120.}

\item{require_same_species}{Logical, require same species before/after the
NONE block? Default TRUE.}
}
\value{
A tibble with one row per detected boundary:
grouping columns, \code{image_id}, \code{image_date_time}, \code{species_common_name},
\code{gap_class = "N"}. If none are found, returns an empty tibble and warns.
}
\description{
Flags boundaries where an animal image is followed (after one or more rows of
\code{NONE}) by another animal image within a short time window. These should be
split into separate series because the intervening \code{NONE} indicates the animal
left the FOV, even if the total animal-to-animal image gap is small.
}
\seealso{
\code{\link[=cam_calc_time_by_series]{cam_calc_time_by_series()}} which accepts the output of this function
via its \code{n_gap_df} parameter to force series splits at NONE-bridged boundaries.
}
\author{
Marcus Becker
}
